---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: centos/7
    provisioner:
      require_chef_omnibus: 12.8.1
    driver:
      network:
        - ['private_network', {ip: '10.1.1.10'}]
      gui: false

suites:
  - name: default
    run_list:
      - recipe[bob_jenkins::default]
    attributes:
